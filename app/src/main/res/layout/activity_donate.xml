<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Header for Donate Food -->
        <TextView
            android:id="@+id/tvDonateHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Donate Food at Annapurna Seva"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="24dp" />

        <!-- Input Field: Food Name -->
        <EditText
            android:id="@+id/etFoodName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Food Name"
            android:padding="12dp"
            android:layout_marginBottom="16dp" />

        <!-- Input Field: Food Description -->
        <EditText
            android:id="@+id/etFoodDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Food Description"
            android:padding="12dp"
            android:layout_marginBottom="16dp" />

        <!-- Input Field: Food Price -->
        <EditText
            android:id="@+id/etFoodPrice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Price (in INR)"
            android:inputType="numberDecimal"
            android:padding="12dp"
            android:layout_marginBottom="16dp" />

        <!-- Input Field: Food Image URL (optional) -->
        <EditText
            android:id="@+id/etFoodImageUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Image URL (optional)"
            android:padding="12dp"
            android:layout_marginBottom="24dp" />

        <!-- Button to Post Donation -->
        <Button
            android:id="@+id/btnPostDonation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Post Donation"
            android:backgroundTint="@android:color/holo_green_dark"
            android:textColor="@android:color/white" />
    </LinearLayout>
</ScrollView>